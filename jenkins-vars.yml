---

# jenkins variables
project_name: docker-unifi
external_type: na
custom_version_command: "curl -sX GET http://dl-origin.ubnt.com/unifi/debian/dists/unifi-5.9/ubiquiti/binary-amd64/Packages |grep -A 7 -m 1 'Package: unifi' | awk -F ': ' '/Version/{print $2;exit}' | awk -F '-' '{print $1}'"
release_type: prerelease
release_tag: 5.9
ls_branch: 5.9
repo_vars:
  - BUILD_VERSION_ARG = 'UNIFI_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-unifi'
  - CONTAINER_NAME = 'unifi'
  - DOCKERHUB_IMAGE = 'linuxserver/unifi'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/unifi'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/unifi'
  - DIST_IMAGE = 'ubuntu'
  - MULTIARCH='true'
  - CI='true'
  - CI_WEB='true'
  - CI_PORT='8443'
  - CI_SSL='true'
  - CI_DELAY='120'
  - CI_DOCKERENV='TZ=US/Pacific'
  - CI_AUTH='user:password'
  - CI_WEBPATH=''
